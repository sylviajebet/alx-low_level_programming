#include <stdio.h>

/**
 * main - Function that writes its own name
 * @argc: Argument count
 * @argv: Array of pointers to argument
 * Return: 0 for success
 **/

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
